# Implementation Completeness Checklist: Release Readiness — Benchmarks, Examples, Docs, CI Gates & NuGet Packaging

**Purpose**: Validate that all spec requirements have been correctly and completely implemented
**Created**: 2026-02-18
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md) | [data-model.md](../data-model.md) | [tasks.md](../tasks.md)

## One-Step Install and First Run (US1)

- [x] CHK001 Is FR-001 fully implemented: both NeatSharp and NeatSharp.Gpu NuGet packages include complete metadata — PackageId, Version (0.1.0), Authors (`hdtorresr`), Description, PackageLicenseExpression (`MIT`), RepositoryUrl (`https://github.com/hdtorrer/neatsharp`), PackageTags, and PackageReadmeFile (embedded README)? A LICENSE file with MIT license text exists at the repository root? [Completeness, Spec §FR-001]
- [x] CHK002 Is FR-002 fully implemented: version number `0.1.0` is managed from a single location (`Directory.Build.props`), both packages inherit and share this version, and the `<Version>0.1.0</Version>` element exists in `Directory.Build.props`? [Completeness, Spec §FR-002]
- [x] CHK003 Is FR-003 fully implemented: non-library projects (tests, samples, benchmarks, tools) are excluded from NuGet packaging via `<IsPackable>false</IsPackable>` default in `Directory.Build.props`, and only `src/NeatSharp/NeatSharp.csproj` and `src/NeatSharp.Gpu/NeatSharp.Gpu.csproj` override to `<IsPackable>true</IsPackable>`? [Completeness, Spec §FR-003]
- [x] CHK004 Is FR-004 fully implemented: a developer can produce NuGet packages using `dotnet pack NeatSharp.sln --configuration Release` with no special tooling beyond the .NET SDK? [Completeness, Spec §FR-004]

## Runnable Example Suite (US2)

- [x] CHK005 Is FR-005 fully implemented: the XOR example in `samples/NeatSharp.Samples/Program.cs` trains a NEAT network to solve XOR, outputs per-generation progress metrics (generation number, best fitness, species count, population size / average nodes / average connections), and prints a final summary showing the champion's accuracy on all four input combinations (`XOR(0,0)`, `XOR(0,1)`, `XOR(1,0)`, `XOR(1,1)`)? [Completeness, Spec §FR-005]
- [x] CHK006 Is FR-006 fully implemented: the Sine Approximation example in `samples/NeatSharp.Samples/Program.cs` trains a NEAT network to approximate the sine function and reports approximation quality metrics (expected vs. output vs. error for sample points, per-generation best/average fitness)? [Completeness, Spec §FR-006]
- [x] CHK007 Is FR-007 fully implemented: the Cart-Pole example in `samples/NeatSharp.Samples/CartPole/CartPoleExample.cs` demonstrates NEAT applied to a sequential control task — the cart-pole system uses 4 inputs (x, x_dot, theta, theta_dot) and 1 output (force direction), with fitness = steps_survived / maxSteps, and outputs per-generation metrics and champion performance (steps balanced, final state)? [Completeness, Spec §FR-007]
- [x] CHK008 Is FR-008 fully implemented: each example (XOR, Sine, Cart-Pole) runs independently without requiring the other examples — XOR and Sine run via `dotnet run --project samples/NeatSharp.Samples`, Cart-Pole runs via `dotnet run -- cart-pole`, and each completes within 60 seconds on a modern machine? [Completeness, Spec §FR-008]
- [x] CHK009 Is FR-009 fully implemented: each example includes inline comments or console output explaining what is happening at each major step — configuration, training, and evaluation of results — including explanatory `Console.WriteLine` headers and inline code comments? [Completeness, Spec §FR-009]
- [x] CHK010 Does `CartPoleSimulator` correctly implement the Barto/Stanley Euler integration equations: computing `theta_ddot` and `x_ddot` from force, mass, gravity, and pole length, then updating state via `x += dt * x_dot`, `x_dot += dt * x_ddot`, `theta += dt * theta_dot`, `theta_dot += dt * theta_ddot`? [Correctness, Data Model §CartPoleState]
- [x] CHK011 Does `CartPoleSimulator.IsFailed()` correctly check both failure conditions: `|X| > TrackHalfLength (2.4)` for cart off track AND `|Theta| > FailureAngle (0.2094)` for pole fallen? [Correctness, Data Model §CartPoleState]
- [x] CHK012 Does `CartPoleSimulator.Reset()` correctly clear state to all zeros (X=0, XDot=0, Theta=0, ThetaDot=0)? [Correctness, Data Model §CartPoleState]
- [x] CHK013 Are Cart-Pole physics unit tests implemented in `tests/NeatSharp.Tests/Examples/CartPoleSimulatorTests.cs` covering: Euler integration with known inputs, cart-off-track failure, pole-angle failure, initial state is all zeros, Reset clears state, and custom config parameters are respected? [Coverage, Tasks §T009]

## README and Quickstart Documentation (US3)

- [x] CHK014 Is FR-021 fully implemented: the README includes a project description, feature highlights (GPU-accelerated, multi-targeted .NET 8/9, deterministic CPU, hybrid evaluation, checkpointing, DI-based configuration), installation instructions for CPU-only (`dotnet add package NeatSharp`) and GPU (`dotnet add package NeatSharp.Gpu`), a copy-paste quickstart code snippet, a GPU quickstart variant, links to all three examples in `samples/`, links to conceptual documentation in `docs/`, and a troubleshooting section? [Completeness, Spec §FR-021]
- [x] CHK015 Does the README quickstart code snippet demonstrate a complete working example: DI setup via `ServiceCollection` + `AddNeatSharp()`, configuring `InputCount`, `OutputCount`, `PopulationSize`, `Seed`, stopping criteria, resolving `INeatEvolver`, calling `RunAsync` with a fitness function, and printing results? [Correctness, Spec §FR-021]
- [x] CHK016 Does the README contain all four required section headings verified by the CI docs workflow: `## Installation`, `## Quickstart`, `## Examples`, `## Troubleshooting`? [Completeness, Spec §FR-013, §FR-021]

## CI Gates for Quality Assurance (US4)

- [x] CHK017 Is FR-010 fully implemented: the CI workflow in `.github/workflows/ci.yml` builds and tests on both Windows (`windows-latest`) and Linux (`ubuntu-latest`) using a matrix strategy with both target frameworks (`net8.0` and `net9.0`), producing 4 matrix cells? [Completeness, Spec §FR-010]
- [x] CHK018 Is FR-011 fully implemented: the CI workflow includes a `format` job that runs `dotnet format NeatSharp.sln --verify-no-changes --severity warn` and fails the build when code does not conform, with `.editorconfig` rules including IDE0055 (formatting), IDE0011 (braces), IDE0161 (file-scoped namespaces), IDE0040 (accessibility modifiers), and `EnforceCodeStyleInBuild` set to `true` in `Directory.Build.props`? [Completeness, Spec §FR-011]
- [x] CHK019 Is FR-012 fully implemented: the CI workflow includes a `pack` job that runs `dotnet pack --configuration Release --output ./artifacts/packages` and uploads `.nupkg` files as a build artifact via `actions/upload-artifact@v4`? [Completeness, Spec §FR-012]
- [x] CHK020 Is FR-013 fully implemented: the CI workflow in `.github/workflows/docs.yml` includes a `validate-readme` job that checks for broken links using `lycheeverse/lychee-action` and verifies required README sections (Installation, Quickstart, Examples, Troubleshooting) via `grep`? [Completeness, Spec §FR-013]
- [x] CHK021 Is FR-014 implemented: the CI pipeline is configured with branch protection rules so that all required status checks (Format Check, all Build & Test matrix cells, Pack) must pass before merging to `main`, with the `gh api` command documented in `CONTRIBUTING.md` for reproducibility? [Completeness, Spec §FR-014]
- [x] CHK022 Is FR-015 fully implemented: the CI pipeline handles GPU absence gracefully — test job uses `--filter "Category!=GPU"` to exclude GPU tests, benchmark job uses `continue-on-error: true`, and GPU benchmarks use a skip flag (`_skipGpu`) when `GpuDeviceDetector.Detect()` returns null? [Completeness, Spec §FR-015]
- [x] CHK023 Does the CI workflow use concurrency grouping (`group: ${{ github.workflow }}-${{ github.ref }}` with `cancel-in-progress: true`) and NuGet caching via `actions/cache@v4` keyed on `*.csproj` and `Directory.Build.props` hashes? [Correctness, Tasks §T016]
- [x] CHK024 Does the `docs.yml` workflow trigger only on pull requests to `main` when `README.md` or `docs/**` files change, with a 3-minute timeout? [Correctness, Tasks §T017]
- [x] CHK025 Does the `build-and-test` job use `fail-fast: false` to ensure all matrix cells run even when one fails, providing complete feedback to contributors? [Correctness, Tasks §T016]

## Benchmark Harness with Regression Guardrails (US5)

- [x] CHK026 Is FR-016 fully implemented: the benchmark suite in `benchmarks/NeatSharp.Benchmarks/` measures CPU and GPU evaluation throughput across at least three population sizes (`[Params(150, 500, 1000, 5000)]`) using BenchmarkDotNet with automated warmup, multiple iterations, and reported mean/standard deviation (`[MemoryDiagnoser]`)? [Completeness, Spec §FR-016]
- [x] CHK027 Does `CpuEvaluatorBenchmarks` use fixed-seed genome generation (`new Random(42)`) in `[GlobalSetup]`, with a `[Benchmark]` method calling batch evaluation and `[BenchmarkCategory("CPU", "CI")]` for CI filtering? [Correctness, Tasks §T020]
- [x] CHK028 Does `GpuEvaluatorBenchmarks` detect GPU availability via `GpuDeviceDetector` in `[GlobalSetup]`, set a `_skipGpu` flag if no GPU is available, return immediately from the benchmark when skipped, use `[BenchmarkCategory("GPU")]`, and include `[GlobalCleanup]` for GPU resource disposal? [Correctness, Tasks §T021]
- [x] CHK029 Does `HybridEvaluatorBenchmarks` compare three partition policies (`Static`, `Adaptive`, `CostBased`) via `[Params]`, use ServiceCollection-based DI setup in `[GlobalSetup]`, and include `[GlobalCleanup]` for provider disposal? [Correctness, Tasks §T022]
- [x] CHK030 Is FR-017 fully implemented: the BenchmarkDotNet entry point in `benchmarks/NeatSharp.Benchmarks/Program.cs` uses `BenchmarkSwitcher` supporting `--filter` and `--exporters json` for machine-readable JSON output? [Completeness, Spec §FR-017]
- [x] CHK031 Is FR-018 fully implemented: the CI benchmark job runs `--filter "*CI*"` (lightweight subset), uses `continue-on-error: true` (no hard-fail gate), and uploads JSON results as an artifact for trend visibility? The benchmark-compare tool accepts `--baseline`, `--current`, and `--threshold` arguments, and defaults to a 10% degradation threshold? [Completeness, Spec §FR-018]
- [x] CHK032 Does the benchmark-compare tool correctly compute percentage change as `((current.Mean - baseline.Mean) / baseline.Mean) * 100`, match benchmarks by `FullName`, report all comparisons with benchmark name/baseline Mean/current Mean/delta, exit code 1 if any benchmark degrades beyond threshold, and exit code 0 otherwise? [Correctness, Data Model §BenchmarkResult]
- [x] CHK033 Is FR-019 fully implemented: a benchmark baseline file exists at `benchmarks/baseline.json` in version control, following the BenchmarkDotNet JSON format with a `Benchmarks` array containing `FullName` and `Statistics.Mean` fields? [Completeness, Spec §FR-019]
- [x] CHK034 Is FR-020 fully implemented: both `GpuEvaluatorBenchmarks` and `HybridEvaluatorBenchmarks` skip GPU benchmarks gracefully when no GPU is available (checking `_skipGpu` flag and returning immediately), running only CPU benchmarks? [Completeness, Spec §FR-020]
- [x] CHK035 Are benchmark-compare tool unit tests implemented in `tests/NeatSharp.Tests/Tools/BenchmarkCompareTests.cs` covering: matching by FullName, correct percentage change calculation, exit code 1 for regression exceeding threshold, exit code 0 within threshold, benchmarks missing in current, malformed JSON handling, and empty baseline handling? [Coverage, Tasks §T023]
- [x] CHK036 Does the benchmarks project (`NeatSharp.Benchmarks.csproj`) target `net9.0`, reference BenchmarkDotNet, and reference both `NeatSharp` and `NeatSharp.Gpu` as project dependencies? Is it excluded from NuGet packaging (inherits `IsPackable=false` from `Directory.Build.props`)? [Completeness, Tasks §T018]
- [x] CHK037 Does the benchmark-compare tool (`tools/benchmark-compare/`) target `net9.0` and use only `System.Text.Json` (no additional dependencies)? [Completeness, Tasks §T023a]

## Conceptual Documentation and Troubleshooting (US6)

- [x] CHK038 Is FR-022 fully implemented: `docs/neat-basics.md` covers NEAT algorithm basics as applied to NeatSharp — genomes (NodeGene, ConnectionGene), species and speciation (CompatibilityDistance), innovation numbers (InnovationTracker), complexification (AddNodeMutation, AddConnectionMutation), and the training loop (NeatEvolver)? [Completeness, Spec §FR-022]
- [x] CHK039 Is FR-022 fully implemented: `docs/parameter-tuning.md` covers key `NeatSharpOptions` parameters (population size, mutation rates, speciation thresholds, stagnation limits), recommended starting values for classification vs. approximation vs. control tasks, and adjustment strategies? [Completeness, Spec §FR-022]
- [x] CHK040 Is FR-022 fully implemented: `docs/reproducibility.md` covers CPU determinism guarantees with `options.Seed`, GPU floating-point non-determinism (ILGPU parallel reduction order), epsilon tolerance for GPU comparisons, seed usage patterns, and how to combine seeds with checkpointing for resumable experiments? [Completeness, Spec §FR-022]
- [x] CHK041 Is FR-022 fully implemented: `docs/checkpointing.md` covers `ICheckpointSerializer` API (SaveAsync/LoadAsync), save/restore workflow with code examples, versioned checkpoint format (SchemaVersion, compatibility), `ICheckpointValidator` for integrity checks, and resuming interrupted training with determinism? [Completeness, Spec §FR-022]
- [x] CHK042 Is FR-022 fully implemented: `docs/gpu-setup.md` covers CUDA toolkit and driver prerequisites, `NeatSharp.Gpu` package installation, `GpuOptions` configuration via `AddNeatSharpGpu()`, `IGpuDeviceDetector` for device discovery, performance tips (batch sizing, population thresholds), and hybrid evaluation via `HybridOptions`? [Completeness, Spec §FR-022]
- [x] CHK043 Is FR-023 fully implemented: `docs/troubleshooting.md` provides step-by-step resolutions for GPU not detected (driver check, CUDA toolkit, compute capability, CUDA runtime libraries), driver/toolkit version mismatch (compatibility matrix), out-of-memory errors (VRAM check, reduce population, hybrid evaluation, pre-allocate cap), common training stalls (premature convergence, no fitness improvement, species explosion), and .NET runtime version issues (build errors, runtime exceptions, reproducibility differences)? [Completeness, Spec §FR-023]
- [x] CHK044 Is FR-024 fully implemented: `docs/offline-usage.md` explains that all core and GPU functionality works offline after initial `dotnet restore`, NuGet restore requires connectivity for new projects, and provides guidance on configuring local NuGet package sources for air-gapped environments? [Completeness, Spec §FR-024]

## Contribution Guide and Release Process (US7)

- [x] CHK045 Is FR-025 fully implemented: `CONTRIBUTING.md` explains development environment setup (prerequisites: .NET 8/9 SDK, Git, optional CUDA), how to build (`dotnet build NeatSharp.sln`), how to test (`dotnet test --filter "Category!=GPU"`), code style requirements (`.editorconfig`, `dotnet format`), branch naming conventions, and PR submission process? [Completeness, Spec §FR-025]
- [x] CHK046 Is FR-026 fully implemented: `.github/pull_request_template.md` includes sections for Description, Spec Impact, Spec IDs, Testing Performed, and a Quality Checklist with checkboxes for: builds on both TFMs, tests pass, `dotnet format` clean, docs updated if applicable, no new warnings? [Completeness, Spec §FR-026]
- [x] CHK047 Is FR-027 fully implemented: `CONTRIBUTING.md` includes a Release Process section documenting the step-by-step release checklist — update Version in `Directory.Build.props`, update `CHANGELOG.md`, verify CI gates, run benchmarks locally, `dotnet pack`, inspect `.nupkg`, test local installation, `dotnet nuget push` to nuget.org, and create GitHub release with tag? [Completeness, Spec §FR-027]
- [x] CHK048 Is FR-028 fully implemented: `CHANGELOG.md` follows Keep-a-Changelog format with `## [0.1.0] - Unreleased` header, categorizes changes (Added), and includes spec ID references (e.g., `001-core-api-baseline`, `008-release-readiness`) linking entries back to feature specifications? [Completeness, Spec §FR-028]

## Success Criteria Verification

- [x] CHK049 Is SC-001 testable: can a developer go from zero to running a NeatSharp example in under 5 minutes — install via `dotnet add package NeatSharp`, paste the README quickstart snippet, and run it with no source cloning, manual builds, or undocumented steps? [Measurability, Spec §SC-001]
- [x] CHK050 Is SC-002 testable: do all three examples (XOR, Sine Approximation, Cart-Pole) run to completion without errors on a fresh clone, each completing within 60 seconds? [Measurability, Spec §SC-002]
- [x] CHK051 Is SC-003 testable: does the CI pipeline detect and block PRs with build failures, test failures, or formatting violations on both Windows and Linux, with pipeline completion within 10 minutes — verified by the `build-and-test` matrix (4 cells × 15-min timeout), `format` job (5-min timeout), and `pack` job (5-min timeout)? [Measurability, Spec §SC-003]
- [x] CHK052 Is SC-004 testable: can a release candidate NuGet package be produced locally via `dotnet pack --configuration Release` and also in CI as a build artifact uploaded by the `pack` job? [Measurability, Spec §SC-004]
- [x] CHK053 Is SC-005 testable: does the benchmark-compare tool detect a 10% throughput degradation (default threshold) against the contributor's own baseline, outputting `FAIL: One or more benchmarks regressed beyond 10% threshold.` with per-benchmark name, baseline Mean, current Mean, and percent change, and exiting with code 1? Does the CI benchmark job report throughput numbers via JSON artifact upload? [Measurability, Spec §SC-005]
- [x] CHK054 Is SC-006 testable: does `docs/troubleshooting.md` cover all three GPU scenarios (not detected, driver mismatch, out-of-memory) with step-by-step resolution instructions, enabling self-service diagnosis without external support? [Measurability, Spec §SC-006]
- [x] CHK055 Is SC-007 testable: can a new contributor set up their development environment, build, test, and submit a well-formed PR by following `CONTRIBUTING.md` (prerequisites, clone, build, test, format, branch naming, PR process) and the PR template (Description, Spec Impact, Spec IDs, Testing, Quality Checklist)? [Measurability, Spec §SC-007]
- [x] CHK056 Is SC-008 testable: are all five conceptual documentation topics covered — NEAT basics (`docs/neat-basics.md`), parameter tuning (`docs/parameter-tuning.md`), reproducibility and determinism (`docs/reproducibility.md`), checkpointing and serialization (`docs/checkpointing.md`), and GPU setup and configuration (`docs/gpu-setup.md`) — and does each guide enable a developer to accomplish its stated task? [Measurability, Spec §SC-008]

## Edge Case Coverage

- [x] CHK057 Is the unsupported-runtime edge case handled: the NuGet packages specify supported target frameworks (`net8.0;net9.0` in both `.csproj` files) so the package manager prevents installation on unsupported runtimes (e.g., .NET 7)? [Coverage, Spec §Edge Cases]
- [x] CHK058 Is the CI-without-GPU edge case handled: GPU-specific tests are excluded via `--filter "Category!=GPU"` in the `build-and-test` job, the `benchmark` job uses `continue-on-error: true`, and GPU benchmarks check `_skipGpu` and return immediately — so CI passes on GPU-less runners? [Coverage, Spec §Edge Cases]
- [x] CHK059 Is the benchmark-on-different-hardware edge case handled: contributors maintain their own local baselines (the `benchmarks/baseline.json` file is the reference but contributors compare against their own runs), the comparison tool computes percentage change from the contributor's own baseline (not a shared absolute), and CI reports numbers for trend visibility only (no hard-fail)? [Coverage, Spec §Edge Cases]
- [x] CHK060 Is the unpublished-NuGet-version edge case handled: the README quickstart references package names without hardcoding a specific version (e.g., `dotnet add package NeatSharp` without `--version`), and `CONTRIBUTING.md` documents how to use local package sources for pre-release testing (step 7 of the release checklist)? [Coverage, Spec §Edge Cases]
- [x] CHK061 Is the formatting-rule-conflict edge case handled: the initial formatting was applied as a one-time reformatting commit (T003) before CI format gate activation, and subsequent CI checks enforce formatting consistently on all files via `dotnet format NeatSharp.sln --verify-no-changes --severity warn`? [Coverage, Spec §Edge Cases]

## Configuration & Validation

- [x] CHK062 Are all CartPoleConfig default values correctly set per the data model: `Gravity=9.8`, `CartMass=1.0`, `PoleMass=0.1`, `PoleHalfLength=0.5`, `ForceMagnitude=10.0`, `TimeStep=0.02`, `MaxSteps=10_000`, `TrackHalfLength=2.4`, `FailureAngle=0.2094`? [Completeness, Data Model §CartPoleConfig]
- [x] CHK063 Are all CartPoleState fields correctly defined per the data model: `X` (double, initial 0.0), `XDot` (double, initial 0.0), `Theta` (double, initial 0.0), `ThetaDot` (double, initial 0.0) — as a mutable struct with default initialization to zero? [Completeness, Data Model §CartPoleState]
- [x] CHK064 Are all NuGet package metadata properties correctly set per the data model: shared properties in `Directory.Build.props` (Version=0.1.0, Authors=hdtorresr, PackageLicenseExpression=MIT, RepositoryUrl, RepositoryType=git, Copyright, PackageProjectUrl), and per-package properties in the `.csproj` files (PackageId, Description, PackageTags, PackageReadmeFile with embedded README)? [Completeness, Data Model §NuGetPackageMetadata]
- [x] CHK065 Are all CI pipeline gates correctly configured per the data model: Format (blocks merge), Build (blocks merge), Test (blocks merge, `--filter "Category!=GPU"`), Pack (blocks merge), Benchmark (`continue-on-error: true`, does NOT block merge), README Links (blocks merge), and README Sections (blocks merge)? [Completeness, Data Model §CIPipelineGates]
- [x] CHK066 Is the `.editorconfig` correctly configured with the specified IDE rules: IDE0055 (formatting, warning), IDE1006 (naming, suggestion), IDE0011 (braces, warning), IDE0160/IDE0161 (file-scoped namespaces, warning), IDE0040 (accessibility modifiers, warning), IDE0007/IDE0008 (var preferences, suggestion), and naming conventions (`_camelCase` private instance, `s_camelCase` private static, `PascalCase` constants/types)? [Completeness, Tasks §T001]
- [x] CHK067 Is the `.gitattributes` correctly configured with `* text=auto eol=lf` for cross-platform line ending normalization, plus C#-specific diff driver and binary rules for images/packages? [Completeness, Tasks §T002]

## Cross-Spec Integration

- [x] CHK068 Does the Cart-Pole example correctly use the existing NeatSharp DI pipeline: `AddNeatSharp()` for service registration, `INeatEvolver` for evolution, `IRunReporter` for summary generation, and `Genome.Activate()` for network evaluation — all from specs 001-004? [Consistency, Spec §FR-007, Spec 001-004]
- [x] CHK069 Do the GPU benchmarks correctly use the existing `GpuBatchEvaluator`, `GpuDeviceDetector`, `GpuOptions`, `IGpuFitnessFunction`, `GpuNetworkBuilder`, and `FeedForwardNetworkBuilder` from specs 006 and 002? [Consistency, Spec §FR-016, Spec 002/006]
- [x] CHK070 Do the hybrid benchmarks correctly use the existing hybrid evaluation infrastructure: `AddNeatSharp()` + `AddNeatSharpGpu()` + `AddNeatSharpHybrid()`, `SplitPolicyType` enum (Static, Adaptive, CostBased), `IBatchEvaluator`, and `IEvaluationStrategy` from spec 007? [Consistency, Spec §FR-016, Spec 007]
- [x] CHK071 Does the conceptual documentation correctly reference types and APIs from earlier specs: `ICheckpointSerializer`, `ICheckpointValidator`, `TrainingCheckpoint`, `SchemaVersion` (spec 005), `GpuOptions`, `IGpuDeviceDetector`, `GpuDeviceDetector` (spec 006), `HybridOptions`, `SplitPolicyType` (spec 007), `NeatSharpOptions`, `INeatEvolver`, `IGenome`, `Genome`, `NodeGene`, `ConnectionGene` (specs 001-002)? [Consistency, Spec §FR-022]

## Setup & Infrastructure

- [x] CHK072 Was the one-time reformatting applied via `dotnet format NeatSharp.sln` as a standalone commit before CI format gate activation, ensuring existing code conforms to `.editorconfig` rules? [Completeness, Tasks §T003]
- [x] CHK073 Does `Directory.Build.props` set `<EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>` to enforce `.editorconfig` rules at build time, in addition to the CI `dotnet format` check? [Completeness, Tasks §T004]
- [x] CHK074 Is the benchmarks project added to `NeatSharp.sln` in a `benchmarks` solution folder, and is the benchmark-compare tool added in a `tools` solution folder? [Completeness, Tasks §T018, §T023a]
- [x] CHK075 Does the `cart-pole` command in `samples/NeatSharp.Samples/Program.cs` correctly invoke `CartPoleExample.RunAsync()` alongside existing commands (default XOR+Sine, `benchmark`, `hybrid-benchmark`)? [Completeness, Tasks §T013]

## Notes

- Check items off as completed: `[x]`
- Add comments or findings inline when an item reveals a gap
- Items reference spec sections (`Spec §FR-XXX`), success criteria (`Spec §SC-XXX`), data model (`Data Model §Section`), tasks (`Tasks §TXXX`), or cross-spec dependencies (`Spec NNN`)
- 75 total items covering 28 functional requirements, 8 success criteria, 5 edge cases
- Traceability: 100% of items include at least one source reference
